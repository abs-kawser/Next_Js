import React from 'react';

const Header = () => {
    return (
        <div>
            <h1 className="text-center bg-primary text-light p-1">header component</h1>
        </div>
    );
};

export default Header;